# GitHub PR Management - Environment Variables

# Encryption Secret for GitHub Tokens
# Generate a secure random string using one of these methods:
# 
# Method 1 (Node.js):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Method 2 (OpenSSL):
# openssl rand -hex 32
#
# Method 3 (Online): https://www.random.org/strings/
# - Length: 64 characters
# - Character set: Alphanumeric
#
# IMPORTANT: 
# - Use DIFFERENT secrets for development and production
# - Keep this secret secure and never commit it to git
# - If compromised, change immediately and re-encrypt all tokens

VITE_ENCRYPTION_SECRET=your-64-character-random-string-here

# Supabase Configuration (if not using default)
# VITE_SUPABASE_URL=your-supabase-url
# VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
